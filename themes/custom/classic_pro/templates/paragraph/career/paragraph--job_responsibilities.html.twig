<h3>{{ content.field_title.0 }}</h3>
    <ul class="list-style-1">
        {% for key,value in content.field_responsibilities['#items'] %}
            <li><span><i class="fas fa-check"></i></span><p>{{ content.field_responsibilities[key] }}</p></li>
        {% endfor %}
    </ul>

            <div class="full-width">
                <img class="service-image" src="{{ file_url(content.field_image['#items'].entity.uri.value) }}" alt="{{content.field_image['#items'].alt}}">
                <div class="content">
                    {{ content.body.0 }}
                </div>
                    {{ content.field_service_lists }}
            </div>
       